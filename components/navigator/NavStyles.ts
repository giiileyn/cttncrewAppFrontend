import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  tabBar: {
    backgroundColor: 'white',
    height: 60,
  },
  icon: {
    width: 60,
    height: 24,
  },
  activeIcon: {
    tintColor: '#fcbf49',
  },
  inactiveIcon: {
    tintColor: '#ccc',
  },
});
